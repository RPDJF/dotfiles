# exports
export EDITOR=vim
DEBUGINFOD_URLS="https://debuginfod.archlinux.org" # for valgrind
QT_QPA_PLATFORM="wayland" # for some compatibilities

# alias
alias shufflebg="bash ~/.scripts/background.sh"
alias waybar-reload="pkill waybar && hyprctl dispatch exec waybar"

shufflebg
fastfetch
source ~/.myconfig/bashrc/prompt.sh

